#include "../minishell.h"

int hpipe(t_data *data, t_dlist *token, char *s, int i)
{
	// if(ft_strlen(token->content) != 0)
	// 	ft_dlstback(&data->cmd_list, ft_strdup(""));
	// token = ft_dlstlast(data->cmd_list); // why??!?!
    // (void) data;
    ft_dlstiter(data->cmd_list, f);
	if (!(ft_strcmp(token->prev->content, "|")))
	{
		printf("errro: %s\n", (char *) token->content);
		exit(1);
	}
	token->content = ft_append(token->content, s[i], -1);
	token->type = PIPE;
    i++;
	return (i);
}
