#include "minishell.h"

int child(int infile, char **cmd, int outfile)
{

}

void redir_setup(t_data *data)
{
    int infile;
    int outfile;
    int curr;

    curr = data->cmds;
    while (curr != NULL)
    {
        curr
    }
}